{
  "name": "pawpoint-directory",
  "version": "1.0.0",
  "description": "A directory for pet owners",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
